2026-05-16: Phase 1 UI should use `refs/UI_ref.png` as the visual target: Manifest3D title anchored left inside top translucent chrome, dominant open WebGPU viewport, right frosted chat/agent panel. No dev server per project AGENTS rule; verify with test/typecheck/lint/build instead.
2026-05-16: Strict UI correction from user: keep phase 1 viewport empty, no mock assets/messages/timeline/status chips/toolbars, no visible viewport container, no grid. Use a plain diffuse white ground and a fixed-corner 3D XYZ gizmo rendered in WebGPU, not a CSS overlay.
2026-05-16: Browser verification screenshots for UI iteration belong under `dev_docs/ui_verification/`, not the project root.
2026-05-16: UI shell should be flattened: WebGPU canvas is full-screen app background, while top chrome and right chat panel are absolute overlays. Avoid a visible workspace/grid container or panel inner-border nesting.
2026-05-16: User does not want browser verification unless explicitly requested. The XYZ gizmo should be Blender-style with high-resolution canvas sprite markers, padded render viewport, and it should animate from open-viewport top-right to full-window top-right when the side panel collapses.
2026-05-16: Approved gizmo direction changed to React Three Drei built-ins: use `GizmoHelper` + `GizmoViewport` for the XYZ gizmo, not custom geometry or CSS overlays. The side-panel collapse state should drive the gizmo margin so it slides between open viewport and full viewport top-right positions.
2026-05-16: Logo direction uses `public/logo.png` plus a responsive glassy SVG wordmark adapted from `refs/logo_demo.html`; keep the top chrome sparse and do not add right-side nav tags or viewport chips.
2026-05-16: Desktop side panel right edge should align with top chrome right edge. Keep the Drei gizmo low enough that its markers do not encroach on the top bar.
2026-05-16: Side panel desktop width should be responsive within min/max bounds, not fixed. To keep the Drei gizmo crisp, use default `GizmoViewport` sizing instead of scaling up `scale`, `axisHeadScale`, or custom font sizing; Voxelit uses this default-size pattern.
2026-05-16: User changed the prompt placeholder copy to `What to create?`; preserve user copy changes unless explicitly asked to alter them. Send icon needs a small optical centering nudge inside its circular button.
2026-05-16: To avoid blurry gizmo labels under the viewport DPR cap, render the Drei `GizmoHelper`/`GizmoViewport` inside a small transparent WebGPU overlay canvas at normal device density and sync it from the main scene camera quaternion. This keeps the main viewport DPR budget intact while making the built-in Drei sprites crisp.
2026-05-16: Desktop side panel bottom inset should match the top chrome top inset so outer UI margins feel consistent.
2026-05-16: The temporary phase-2 `demoManifestScene` runtime fixture was removed after user approval. App startup should use an empty Manifest3D scene until real agent-generated assets are wired in. Export toolbar visibility is no longer selection-gated; it stays in the top chrome and is disabled when no asset is selected.
2026-05-16: The main WebGPU canvas and the separate Drei gizmo overlay now use R3F `frameloop="demand"` so idle scenes stop rendering. When adding animated camera/scene behavior, explicitly call Fiber `invalidate()` while motion is active, as the selection snap loop does.
2026-05-16: Effective viewport centering should treat the visible viewport right boundary as the side panel's left edge. For Three `PerspectiveCamera.setViewOffset`, use a positive `offsetX` equal to half the right occlusion width to move the orbit target left into the visible viewport center.
2026-05-16: Historical V0 validation harness staged candidates as schema -> semantic -> build -> geometry -> prompt tests, then optional commit. Treat this as provisional scaffolding only; the revised plan supersedes it with Contract V2, signal bundles, baseline QC, exact checks, allowances, repair feedback, and freshness.
2026-05-16: UI/renderer are treated as the stable base; schema, validation, prompt tests, agent loop, and export align with Contract V2, signal bundles, baseline QC, exact checks, allowances, repair feedback, and candidate freshness.
2026-05-16: Phase 3 core has been rebuilt around Contract V2: assets now carry stable ids, units, checks, and allowances; parts no longer use `parentId`; the builder assembles via joints; validation emits signal bundles with structural checks, baseline QC, exact checks, overlap/isolation allowances, and export readiness.
2026-05-16: Phase 4 agent infrastructure is local/pure TS: `candidateHistory.ts` gates readiness by active candidate fingerprint matching the latest successful validation attempt; repeated failure signatures are derived from failure signals, not whole reports. `repairFeedback.ts` renders model-facing `<validation_signals>` from bundles and prioritizes schema/structure/build before QC/check repair. Prompt source lives in `src/engine/agent/prompts/*.md` and is imported by `promptCompiler.ts`.
2026-05-16: Manifest3D prompts carry the relevant asset-generation guidance: realistic geometry as the dominant quality bar, real-world scale/materials, primary mechanism articulation, no floating parts, scoped intentional overlaps paired with exact proof checks, stable semantic ids/names, and "validation as sensors, not optimization targets."
2026-05-16: Phase 5 uses `src/engine/config/modelConfig.ts` as the only model settings source and keeps Responses API details isolated in `src/engine/agent/openAiManifestClient.ts`. The OpenAI client sends `text.format.type=json_schema` with strict Manifest3D structured output plus `input_image` payloads; missing `VITE_OPENAI_API_KEY` returns an unavailable result without changing the scene. `agentLoop.ts` records invalid attempts in `candidateHistory`, renders repair feedback for retries, and commits only a fresh valid candidate through `sceneStore.upsertAsset`.
2026-05-17: Persistence now separates the saved asset library from runtime viewports. `assetLibraryStore` persists asset records, version records, and per-version validation attempts to IndexedDB; assets are ordered by original creation time descending, not last update. `sceneStore` is runtime-only: Create has one `create` instance, Compose has many transformed instances. Edit prompts for saved versions include a compact validation-attempt context, not LLM transcripts.
2026-05-17: Agent/UI design choices are captured in `dev_docs/agent_design.md` and `dev_docs/UI_design.md`. Key choices: no persisted LLM transcripts, persist validated asset versions plus per-version attempts, keep Create single-asset and Compose multi-asset/runtime-only, normalize validation timeline messages, and make Compose transform constraints reject below-table scale/rotation rather than auto-lifting.
2026-05-17: Phase 6 GLB export lives in `src/engine/scene/exportGlb.ts`. The top-chrome Export GLB button remains visible but is enabled only when the Create workspace has a viewed `create` instance loaded; it does not depend on the transient renderer outline selection. Compose selections intentionally do not export. Export builds the viewed Create manifest asset into an isolated group, clones it, strips helpers/non-exportable objects/userData, converts WebGPU node materials to standard GLTF-friendly materials, and downloads a binary `.glb`.
2026-05-17: GLB export regression fix: `MeshStandardNodeMaterial` can also present `isMeshStandardMaterial`, so export conversion must explicitly reject node materials before cloning. Otherwise GLTFExporter writes material slots but loses authored color/metalness/roughness and falls back to default PBR values.
2026-05-17: Selection emphasis should not tint asset materials. Use Three's WebGPU/TSL `OutlineNode` pipeline, not per-mesh back-side expansion, so selected assets render as one screen-space mask with a camera-perspective silhouette rather than internal component outlines.
2026-05-17: OpenAI API keys are startup-gated by browser hostname: `VITE_OPENAI_API_KEY` is used only on `localhost`, `127.0.0.1`, or loopback IPv6. Other origins must use the in-app API Key modal, which stores the provided key only in memory for the current browser session.
2026-05-17: When tuning selection outlines, keep the `OutlineNode` edge mask boosted with `edgeGlow`, then use that mask to replace pixels with the outline color. Pure additive composition disappears over the light table plane because bright backgrounds clamp toward white.
2026-05-17: User-approved main viewport DPR tuning now uses `maxPixels = 4_000_000` and a DPR cap of `1.75`; keep renderer tests aligned with those values.
2026-05-17: Selection camera centering must react to the selected Three object becoming available, not only to `selectionRevision`. History opens and fresh Create results can set selection before the asset group registers, so `SelectionCameraTarget` depends on the resolved selected object.
2026-05-18: Engine/harness audit follow-up tightened the OpenAI structured-output schema with vector lengths, positive dimensions, segment ranges, nonnegative check tolerances, material bounds, and minimum generated part/material/visual arrays; keep prompt examples parseable against Contract V2 because they materially steer headless candidate shape.
2026-05-18: Two full live `npm run test:headless` attempts reached only the OpenAI fetch layer and failed after about 301s with `fetch failed`, so they are not asset-quality evidence. `.env` had an OpenAI key entry and the shared app OpenAI client was left unchanged. A shortened `HEADLESS_AGENT_EXPECT_READY=0 HEADLESS_AGENT_RUN_TIMEOUT_MS=5000 HEADLESS_AGENT_TIMEOUT_MS=65000 npm run test:headless` run passed and now records per-exchange duration; use that when diagnosing API reachability separately from validation quality.
2026-05-18: Headless test now uses a headless-only Node `https` fetcher with `HEADLESS_AGENT_FETCH_TIMEOUT_MS` so long Responses calls are not cut off at Node fetch's observed ~301s boundary. The app OpenAI client remains unchanged.
2026-05-18: One-hour headless stress runs passed with real model outputs: toolbox prompt reached ready in 2 attempts after sampled-pose overlap repair; pickup prompt reached ready in 4 attempts after 128/42/40 overlap failures. Main harness pain point was repair-feedback bloat from repeated visual-pair overlap signals (pickup repair prompts were 78k-110k chars), so `repairFeedback` now compacts repeated signal groups for model-facing feedback while keeping the full signal bundle/artifacts intact.
2026-05-18: Headless summaries now record run duration, exchange count, per-exchange duration, prompt character sections, and rough char/4 token estimates. The live prompt matrix covered simple/static chair (ready in 1 attempt, ~2.6k input/~1.6k output tokens), simple/articulated toolbox (ready in 2 attempts, repair ~11.2k input/~3.0k output), difficult/static castle gatehouse (ready in 2 attempts, repair ~30.6k input/~8.6k output), and difficult/articulated pickup (ready in 3 attempts after feedback compaction, repairs ~21.8k-21.9k input/~7.1k-7.6k output). Context is now much less wasteful in validation feedback, but full candidate JSON echo dominates repair turns, especially for high-detail static assets.
2026-05-18: Second harness pass keeps candidate-history freshness as the hard invariant and adds revision/fingerprint repair-context guidance to make that invariant visible to the model. Repair turns now include complete but minified candidate JSON, reducing candidate-context whitespace without dropping geometry; on existing stress artifacts this would shrink castle attempt-1 candidate context from 105,641 to 40,687 chars and pickup attempt-1 from 60,937 to 25,043 chars. App runtime uses the same one-hour run timeout as headless stress. Contract V2 now includes manifest `controls` so generated assets can group multiple joints under one UI dial or fall back to independent joint dials; the joint preview panel scrolls when controls overflow.
2026-05-18: Live post-controls headless schema check passed: simple static chair prompt reached ready in 1 attempt after 218.6s with ~2,953 input and ~1,972 output tokens. The returned static asset used fixed assembly joints and `controls: []`, confirming the updated strict schema is accepted by the API.
2026-05-18: Dynamic GLB export is now selected only for assets with movable joints. Static assets keep the direct Export GLB action; movable assets open a Static/Dynamic menu. Dynamic export generates standard glTF animation clips from manifest controls or fallback joint controls, targeting cloned joint groups with TRS tracks while still exporting the canonical rest asset.
2026-05-18: App startup is desktop-gated in `src/main.tsx`: mobile/tablet browsers receive only the desktop-only message inside a lightweight top frame with the logo image plus the same filtered SVG wordmark design as desktop, and do not import the React/WebGPU app bundle. Create/edit runs are task-scoped rather than global: multiple create jobs can show as top `Creating` rows, edit jobs attach to their source asset row, and opening a running row restores that task's prompt/progress until it finishes. AppShell runs jobs against isolated scene stores so browsing saved assets does not mutate in-flight prompt context; saved assets persist through per-asset IndexedDB writes instead of clearing the whole library snapshot.
2026-05-18: OpenAI API keys must never enter the built client bundle. Vite now uses `envPrefix: 'MANIFEST3D_PUBLIC_'` so `VITE_OPENAI_API_KEY` is not exposed to browser code, and local development reads `.env` keys through the dev-server-only `/__manifest3d/local-openai-api-key` endpoint restricted to localhost host headers. App code should not read `import.meta.env.VITE_OPENAI_API_KEY`.
2026-05-19: The top-bar API Key button now shows readiness with a status dot. Green means a usable key is loaded either from the localhost-only startup `.env` endpoint or from the in-memory session modal; red means no usable key is currently loaded, including localhost startup when `.env` is missing or blank.
2026-05-19: API Key toolbar availability is hostname-based, not readiness-based: localhost/loopback keeps the button visually active-looking but click-locked because local keys must come only from the dev-server `.env` endpoint. Deployed origins keep the button clickable for the in-memory modal. The status dot still reflects actual key readiness, and app code must not read API keys from `import.meta.env` or otherwise put them in the built bundle.

2026-05-19: In Create workspace, asset history selection now represents the viewed Create asset, not the transient renderer object-selection outline. Clearing the viewport outline should not clear the left-panel active asset state, edit-mode right panel, version navigation, or export eligibility while the Create viewport still shows that asset. Pending create runs use the active run id for the highlighted `Creating` row.
2026-05-20: Pipeline quality pass added Contract V2 `roundedBox` and `capsule` geometry primitives so generated assets can represent softened manufactured housings, panels, handles, rails, grips, and rounded pins without excessive box/cylinder stacks. Multi-joint articulated assets now fail validation unless manifest `controls` cover their movable joints; this is an app quality invariant for preview/dynamic GLB behavior, not a headless-test accommodation. Headless GLB artifact export remains self-contained in `test/headless/agentPipelineSmoke.test.ts`.
2026-05-20: Headless image-conditioned fan stress run exposed false-positive overlap pressure from hollow `torus`/closed `tube` visuals: the old broadphase treated a ring/grille AABB as a filled disk, causing repeated guard-vs-rotor failures. `overlapChecks` now splits torus and tube visuals into segment bounds before overlap testing, while still reporting real material intersections. Prompt guidance also calls out clearance for moving rotors/blades/wheels inside stationary guards, grilles, cages, and shrouds.
2026-05-20: Material emission is now an authored material property: materials can carry nullable `emission` and `emissionAnimation` fields, with keyframed on/off, color, intensity, loop, and step/linear interpolation. The renderer applies emission preview state per scene instance, the existing animation panel shows material emission timelines next to joint controls, and dynamic GLB export post-processes binary GLBs with real `KHR_animation_pointer` channels targeting material `emissiveFactor` and `KHR_materials_emissive_strength.emissiveStrength`; static export intentionally omits those animation channels.
2026-05-20: Generated sampled-pose validation now samples manifest preview controls rather than each movable joint in isolation. This better matches UI/export behavior for linked mechanisms such as paired steering or mirrored wheel spin and avoids impossible one-joint repair pressure. Structure validation also fails controls whose limits/bindings clamp to no effective joint motion, because those controls would otherwise look valid while preview/export cannot animate them.
2026-05-20: Pipeline quality pass made intentional overlap allowances enforceable: each `allow_overlap` now needs a matching authored proof check for the same part pair, and visual-scoped allowances need the same visual pair in that proof. Broad overlap allowances stay non-blocking but warn. Prompt/repair guidance now also calls out `roundedBox.radius <= min(size)/2` and asks generated visuals inside one part to touch/mount or be split into fixed child parts. Live headless checks: police-car material-emission prompt reached ready in 1 attempt/338.8s with empty signals and static+dynamic GLBs; articulated pickup reached ready in 3 attempts/820.4s with static+dynamic GLBs, down from the recent comparable 5-attempt/1529.3s run, though it still had one non-blocking disconnected-island warning on `truck-body`.
2026-05-20: Provider support now includes OpenAI and Gemini. `modelConfig.ts` no longer has a hardcoded provider field; OpenAI keeps the starting default, while the Providers panel caches the last selected provider in localStorage. Local dev keys are served only through `/__manifest3d/local-provider-api-keys` from `.env` (`OPENAI_API_KEY`/legacy `VITE_OPENAI_API_KEY`, `GEMINI_API_KEY`/`GOOGLE_API_KEY`), and non-local origins keep per-provider API keys only in tab memory. Gemini uses the direct GenerateContent REST path with `gemini-flash-latest`, `thinkingLevel: "high"`, temperature 1.0, max output tokens 64k, and the shared Manifest3D JSON schema via `generationConfig.responseMimeType` plus `responseJsonSchema`.
2026-05-20: Pending create rows in the asset history panel should show the literal submitted prompt as their temporary title, with no multi-run numeric suffix; once saved, the normal asset name/version row takes over. Animation preview playback is now per-control, so multiple joint/material bars can play together, while an individual reset stops only that bar.
2026-05-21: Material render side is now an explicit manifest material contract (`front`/`back`/`double`) and the WebGPU renderer maps it directly to Three material side. Missing legacy side defaults to `front`, while strict provider output requires it. Open/cutaway `lathe` visuals are treated as surface-side sensitive and need a matching `expect_material_side` authored check, so paper-thin or one-sided surfaces remain allowed but intentional/testable instead of accidental back-face culling.
2026-05-21: Agent runs remain stateless at the provider transcript layer, but saved asset versions now optionally persist direct submitted user input (`text` plus attached image data/metadata). Follow-up edit prompts prepend selected-version lineage user inputs plus the pending turn before the existing prompt assembly and send all referenced images with the fresh provider request. Saved right-panel transcripts are reconstructed from version lineage when user input exists; legacy rows without user input intentionally avoid fabricating chat turns.
2026-05-22: Viewport world light/dark mode is renderer-scoped: `viewportWorld.ts` owns the bright existing world and the low-light dark world, while the top-left icon control only moves with the left asset panel. Do not tie this to app UI theming or mutate asset materials for the mode.
2026-05-22: Default agent repair turn cap is now 10. Keep app runtime and headless default aligned through `defaultRepairTurnCap`; explicit `maxRepairTurns` overrides and `HEADLESS_AGENT_MAX_REPAIR_TURNS` still take precedence.
- 2026-05-23: On compact navbar widths, toolbar action buttons now truly collapse to icon-only by centering content, hiding the export chevron, and hiding the API key status dot. This keeps Export, API Key, and GitHub visually centered in their circular buttons.
- 2026-05-23: Swapped the viewport key/fill light positions in both light and dark environments so the shadow-casting key light now approaches from the former fill side, matching the previously observed world-space ground/table glint direction more plausibly.
- 2026-05-23: Prompt composer image thumbnails now use the same full-image preview modal as submitted chat images. Keep the composer preview action separate from the remove button to avoid nested interactive controls.
- 2026-05-23: Prompt composer attachment preview strip keeps extra top/right padding because the remove button is intentionally offset outside the thumbnail tile; this avoids scroll-container clipping while keeping the thumbnail click target intact.

- Path tracer viewport mode is intentionally isolated from the default WebGPU/TSL renderer. The default renderer stays mounted and handles existing viewport interaction; when path tracer mode is active its canvas is hidden with opacity while a pointer-events-none WebGL2 path tracing canvas overlays it. This preserves existing OrbitControls/selection/transform behavior while the path traced image is used as the visible render.
- Path tracing lives under `src/renderer/pathtracer/`. It converts Manifest3D's `MeshStandardNodeMaterial` output into plain `MeshStandardMaterial` instances before uploading the scene to `three-gpu-pathtracer`, because the path tracer targets standard/physical Three materials rather than TSL node materials. Path tracer visual tuning is code-only via `pathTracingConfig.ts`, not user-facing knobs.
- 2026-05-23: Path tracer viewport now renders full-frame samples from the first visible path-traced update by using a 1x1 tile layout. Avoid tiled path tracing in the live viewport unless the UI intentionally masks partial tile updates; 2x2 tiles can visibly fill only one quadrant during early convergence.
- 2026-05-23: Path tracer mode shows a sample counter at the top center of the viewport and caps live accumulation through the selected path tracer max-sample target. Animation preview playback is default-renderer-only; switching into path tracer mode clears playing preview controls and hides the animation panel.
- 2026-05-23: Path tracer viewport navigation intentionally disables OrbitControls damping/inertia and uses immediate selection-target snapping. This keeps camera motion settled as soon as user input stops so path-tracing accumulation can restart immediately, while default WebGPU mode preserves the existing damped/animated navigation feel.

- 2026-05-23: Path tracer camera alignment must use the same `PerspectiveCamera.setViewOffset()` projection helper as the default WebGPU viewport. A look-target/quaternion shift was tried and failed visually because it depended on the OrbitControls target being the exact visual asset center; matching the default projection offset keeps the path-traced image centered inside the side-panel-aware effective viewport.
- 2026-05-23: Path tracer fallback camera snapshots must be built from the same default viewport camera constants and must `lookAt` the default OrbitControls target before exporting the quaternion. A position-only fallback with identity quaternion misaligns the visible path-traced canvas from the hidden WebGPU picking/camera viewport until the first mirrored camera snapshot arrives, which breaks apparent centering and click-to-center behavior.
- 2026-05-23: Path tracer camera mirroring must not depend only on passive `useFrame` snapshot polling under the demand-rendered hidden WebGPU canvas. Publish camera snapshots directly from OrbitControls changes and selection-target snaps, forcing a snapshot when path tracer mode immediately recenters a selected asset. Otherwise the visible path-traced overlay can keep rendering a stale/fallback camera while the hidden picking viewport has the corrected camera, causing visible-click and centering mismatches.
- 2026-05-23: `three-gpu-pathtracer`'s static scene generator reads object `matrixWorld` values when baking geometry and does not update the scene graph for us. After rebuilding the path-tracing scene, call `scene.updateMatrixWorld(true)` before upload, and keep the upload path doing the same defensively. Without this, path-traced geometry can be baked from stale/local transforms while the hidden WebGPU picking viewport targets the correctly placed object, causing orbit-center, click-to-center, and effective-viewport centering mismatches.

- Path tracing post-processing viewport reset must use `renderer.getSize()` CSS pixels, not `getDrawingBufferSize()` physical pixels. `WebGLRenderer.setViewport()` already applies DPR for the default framebuffer, so passing drawing-buffer dimensions over-scales/crops the final TexturePass/Bloom composite on Retina/high-DPR screens and makes the path-traced image look like it has a drifted center even when the camera target is correct.
- 2026-05-23: Path tracer bloom is intentionally tuned lower than the default WebGPU bloom because path-traced emissive surfaces can accumulate much hotter HDR values before post-processing. Do not assume identical numeric bloom settings produce identical perceived glare between WebGPU TSL bloom and WebGL UnrealBloomPass.
- 2026-05-23: `three-gpu-pathtracer` treats every supported direct light as physically shadowing; `DirectionalLight.castShadow = false` is not a per-light shadow-disable path there. To match the default viewport's non-shadowing fill behavior, keep only the key directional light in the path-traced direct-light set and approximate fill with a low-intensity gradient environment.
- 2026-05-23: Path tracing render mode controls now live beside the top-left light/dark viewport world controls and follow the left asset panel collapse state, not the right chat panel state. Keep viewport render-mode placement visually paired with viewport world-mode placement.
- 2026-05-23: Path tracer accumulation should not keep a hot `requestAnimationFrame` loop after `pathTracingConfig.maxSamples` is reached. Restart the loop only when camera, viewport size, scene, material/pose preview, world mode, or other path-tracer inputs change. Also keep the sample counter as an imperative DOM text update rather than React state so sample accumulation does not cause React renders every frame.
- 2026-05-23: The default WebGPU/TSL renderer must be sized before `WebGPURenderer.init()` in the R3F `gl` factory. Prefer canvas layout size, then parent layout size, then window size before falling back to canvas backing size, so Three/WebGPU postprocessing attachments do not start from the browser default `300x150` canvas buffer and race the real swapchain size.
- 2026-05-23: `SceneEffectsPipeline` is size/DPR-sensitive. Keep R3F `size` and viewport DPR in its memo dependencies so TSL `RenderPipeline`, MRT, bloom, and outline attachments rebuild after viewport resize, browser zoom, side-panel layout changes, or monitor DPR changes.
- 2026-05-23: Path tracer denoising is final-frame only. `PathTracingCanvas` uses the raw path-tracer target during progressive accumulation and swaps to `pathTracingDenoisePipeline` output only when `pathTracer.samples` reaches the selected max-sample target, before bloom/output. Keep all denoise code under `src/renderer/pathtracer/` and do not couple it to the default WebGPU renderer.
- 2026-05-23: The path tracer denoise pass is non-ML/no-dependency GPU postprocessing: raster normal+depth auxiliaries, firefly clamp, then bounded à-trous passes weighted by color/normal/depth. Keep the pass count conservative; more passes can blur authored edges and increase final-frame latency.
- 2026-05-23: Denoise shader helpers must avoid generic names like `luminance`; Three.js/WebGL shader chunks or generated code can already define them, causing browser-only compile failures even when TS/build checks pass. Use denoise-prefixed GLSL helper names and keep the raw path-tracer texture as the fallback if a denoise WebGL pass reports an error.

- 2026-05-23: The runtime installs a narrow console warning filter for the harmless upstream `THREE.Clock: This module has been deprecated. Please use THREE.Timer instead.` message. Keep the filter exact so real Three.js warnings, especially WebGL shader/program errors, still surface during renderer debugging.

- 2026-05-23: Path tracer sample counter text can change without the numeric sample count changing, e.g. final `max / max samples (denoising)` to `(denoised)`. Keep the imperative publisher keyed by full text content rather than sample count only so status-only overlay updates are not skipped.

2026-05-23: Path tracer final-frame denoising is now a user preference, not automatic. The top-viewport Denoiser toggle is disabled outside path tracer mode, defaults off, persists via `manifest3d:pathtracer-denoiser-enabled`, and only gates the final denoise pass after max samples; raw max-sample output should be labeled `(not denoised)` when the toggle is off.

2026-05-24: Path tracer max samples are now user-selectable from the clickable top-center sample counter and persisted via `manifest3d:pathtracer-max-samples`. Supported targets are 128, 256, and 512, with 128 as the default. Raising the target should continue the current accumulation; lowering the target should not reset the current path-traced texture and should only change when the renderer considers the current/new accumulation complete.

2026-05-24: The path tracer denoiser now relies on richer final-frame guide buffers rather than only raw depth/normal: linearized depth, high-precision normals, material transparency/roughness/emissive protection, and per-object guide keys. The final composite intentionally restores raw or firefly-cleaned path-traced color on silhouettes, transparent surfaces, emissive surfaces, and guide-buffer edges to avoid stair-stepped outlines and DOF-like blur. Keep future denoise tuning in `src/renderer/pathtracer/` and prefer guide-aware protection over simply increasing à-trous pass count or blur radius.

2026-05-24: The denoiser has a separate albedo/metalness guide and rough-opaque diffuse illumination branch. For broad matte surfaces it denoises approximate illumination (`path-traced color / albedo`) and reconstructs with albedo before a residual-aware final composite. This is intended to improve colored indirect/diffuse light splotches without blurring authored base-color edges. Transparent materials are no longer blanket raw-preserved: transparent interiors can receive limited smoothing, while transparent silhouettes/guide edges still preserve raw or firefly-cleaned color.

2026-05-24: Emissive-firefly denoiser recovery is now a two-level gated path, not a global blur increase. `pathTracingDenoiseFireflyRecovery.ts` arms recovery only for compact/very bright emissive scene risk with dark rough diffuse receivers; `pathTracingDenoisePipeline.ts` then builds a raw-frame confidence mask from same-surface luminance outliers. That confidence down-weights firefly samples before diffuse/beauty filtering and only boosts final repair on risky dark rough dielectric receiver pixels. Keep emissive, transparent/glass, metallic/specular, silhouettes, and guide edges protected by guide masks rather than by lowering emission, bloom, tone mapping, or path-tracer lighting.

2026-05-25: High-risk emissive mesh path tracing now has optional real-emitter next-event sampling in `pathTracingEmissiveMeshSampling.ts`. It is independent from the denoiser toggle and only arms for compact/bright opaque emissive mesh triangles plus dark rough diffuse receiver risk. The shader extension samples actual emissive triangles by power-weighted CDF and shadow-tests them; it does not create proxy/helper lights or change emission/bloom/tone mapping. Expect denoiser-off raw output in those risky scenes to become less speckled and more converged at the same sample count, which is a sampling-quality change rather than a denoiser change.
2026-05-25: When patching the `three-gpu-pathtracer` material shader at startup, mark the material dirty through Three's base `Material.needsUpdate` setter instead of `MaterialBase.needsUpdate`. `MaterialBase` dispatches `recompilation`, which can trigger `WebGLRenderer.compileAsync()` before `currentProgram` exists and produce a harmless but noisy `program.isReady` console exception even though the next render compiles successfully.
2026-05-25: Direct OrbitControls input in path tracer mode is intentionally interactive-preview capped. `WebGPUCanvas` keeps a short settled/active flag from OrbitControls start/end events, and `PathTracingCanvas` uses `pathTracingConfig.interaction.activeSampleLimit` as the temporary sample cap while that flag is active. Do not restart full progressive accumulation until the viewport navigation settle delay expires; otherwise pan/orbit/wheel input competes with expensive path-tracing samples and feels laggy.

2026-05-25: Headless agent runs should stay as close as possible to in-app pipeline code: wrap the imported provider client factory only for artifact capture, do not recreate app provider switching or transport/request plumbing in `test/headless`. `HEADLESS_AGENT_PROVIDER` selects OpenAI or Gemini through the app factory. Headless artifacts now default under `test/headless/artifacts/headless-agent/` so `test/headless/glb_viewer.html` can serve them, and schema-parseable attempts export per-attempt GLBs for visual comparison across repair turns.
2026-05-25: OpenAI provider calls now use Responses API background polling by default in the shared app client, not a headless-only transport workaround. The request sets background/store true, then polls the response id and retries transient create/poll failures. This is intended to avoid long idle non-streaming HTTP failures for complex gpt-5.5 asset generations; the tradeoff is background mode response retention, so treat it as a deliberate non-ZDR choice.
2026-05-25: Pipeline repair loop now uses semantic failure clusters, deterministic probe reports, JSON Patch repair responses, and `connectorTube` endpoint geometry. Create/edit provider responses remain full Manifest3D assets; repair responses are `{ "patch": [...] }` and are applied by the harness before validation. `connectorTube` is for chains/cables/hoses/ropes/straps/wires across moving parts and ignores local transform because geometry is regenerated from endpoint part transforms for validation and preview. Probe reports are geometry sensor data, not rendered-image feedback.
2026-05-26: `connectorTube` visuals accept either empty transforms or identity transforms. OpenAI structured output currently emits full visual transforms, so identity transform is the structured-output-compatible representation; connector geometry still resolves only from endpoint part poses.
2026-05-26: Dynamic GLB export now adds morph-target weight tracks for `connectorTube` meshes so chains/cables follow animated endpoint parts in exported diagnostic assets. The app creation loop does not use rendered-image feedback.
2026-05-26: Headless smoke has a diagnostic-only repeated-failure stop controlled by `HEADLESS_AGENT_REPEATED_FAILURE_STOP_STREAK` (default 3, set 0 to disable). It returns a failed result before another model call when the same semantic validation signature repeats, but app runtime behavior remains unchanged.
2026-05-26: Connector overlap QC now subdivides tube/connector segment proxies and treats only endpoint-adjacent connector chunks as allowed to touch their referenced endpoint parts. This is intended as a general flexible-connector rule, not a gatehouse-specific allowance; unrelated obstruction overlaps still fail.
2026-05-26: Repair JSON Patch application now schema-checks the fully patched Manifest3D asset before accepting it as the next candidate. Schema-invalid patches stay as patch-application feedback against the previous candidate instead of corrupting candidate history.
2026-05-26: Patch-application errors now carry repeated-error state in the shared agent loop. Rejected repair patches are not applied, and repeated identical patch errors include the streak plus a compact rejected-operation summary in the next repair prompt so the model can see when it is resending the same bad value.
2026-05-26: Repair patch response schema must allow focused numeric-vector values and must not allow generic empty array values. A live gatehouse run showed OpenAI using `value: []` for `/geometry/size` and `/transform/position` because empty arrays satisfied typed array branches while valid standalone vector replacements were not represented in the patch value schema.
2026-05-26: Gemini repair requests intentionally use a provider-only transport schema instead of the central repair patch schema. The central schema remains authoritative, but sending its full `patch.value` universe to Gemini makes the structured-output schema very large/deep and can trigger `400 INVALID_ARGUMENT` schema-complexity failures. Gemini repair outputs `op`, `path`, and JSON-stringified `valueJson`; `geminiManifestClient.ts` parses that back to canonical JSON Patch before the shared loop applies it and validates the fully patched Manifest3D asset. Do not weaken the central contract or OpenAI schema to match this Gemini transport workaround.

- Default viewport zoom-out readability: keep WebGPU viewport fog-free, and keep the shared default camera far plane comfortably beyond the OrbitControls max zoom-out distance so large assets are not clipped at the far end. Do not solve this by splitting unrelated WebGPU/path-tracer camera behavior; prefer shared camera defaults unless a renderer genuinely needs a separate override.
- 2026-05-26: Default WebGPU contact shadow truncation comes from the key directional light's orthographic shadow camera, not the world plane size. Tune `src/renderer/viewportShadows.ts` (`defaultViewportShadowConfig`) for shadow coverage/map size, and tune `src/renderer/viewportCamera.ts` (`defaultViewportCameraConfig.far`) for camera far clipping. The current camera far plane is 150, deliberately lower than the earlier 500 experiment but still beyond the OrbitControls max zoom-out.

- 2026-05-26: Default WebGPU shadow truncation for tall assets was not only fixed shadow-camera width; the old key directional light used a small fixed position/target, so tall geometry could sit behind or outside the light's shadow camera and produce permanently deformed ground shadows. `viewportShadows.ts` now computes a dynamic key-light shadow state from asset bounds plus their projection onto the ground plane, moves the light along the same authored direction, and fits the orthographic shadow camera around both caster and receiver points. Hand-tune minimum coverage in `defaultViewportShadowConfig`, not by resizing the ground plane.

2026-05-27: Agent progress timelines are now built by interleaving raw agent loop events with candidate validation attempts via `createAgentProgressTimeline`. The app updates the current transcript item in place during and after commit, rather than replacing the active run with a saved attempt-only transcript. Saved versions persist `agentEvents` alongside attempts so history opens can reconstruct the same continuous timeline; versions without events reconstruct from attempt data only. Routine event details stay hidden except concise failed-event messages.
2026-05-27: The path tracer denoiser toggle is path-tracer-only UI. Do not render the Denoiser button in default WebGPU mode; the existing denoiser preference and renderer behavior still only gate the path tracer final-frame denoise pass.

2026-05-27: Agent progress timeline UI is now attempt-sectioned. `createAgentProgressTimeline` emits `Initial attempt` / `Repair N` separators, suppresses the generic completed `Validate candidate` row, shows concise failure summaries only on failed candidate rows, and keeps full validation steps only for successful attempts. Leave the section footer absent while an attempt is still running; append it once that attempt fails or reaches asset-ready success.
2026-05-31: Overlap/contact oscillation pass added shared visual-pair relation metrics in `src/engine/geometry/relationMetrics.ts`. Overlap QC, `expect_contact`, support reachability, and probe reports now share shape-aware proxies; long solid visuals are subdivided along their dominant local axis, while torus/tube/connectorTube keep polyline proxies. `expect_contact` now bounds hidden penetration via optional `maxPenetration`, failed-pair probes report closest visual pair/distance/penetration/overlap, relation-loop hints call out alternating overlap-vs-gap failures for the same part pair, connectorTube support connects endpoint parts in baseline QC, mechanical `allow_isolated_part` allowances are rejected, and repair patches can use stable id virtual paths like `/parts/byId/foo/visuals/byId/bar/...` to avoid stale array-index failures.
2026-05-31: Follow-up headless feedback showed sampled-pose overlap QC could still be world-frame dependent: a fan head and blade rotor that yaw together kept gaining false overlaps only at yaw samples. Sampled-pose validation now suppresses new overlap findings for part pairs whose relative transform is unchanged from rest pose, while still checking true relative-motion pairs. Probe reports now measure sampled-pose failed relations in the failing pose when joint values are available, and patch-application feedback adds targeted hints for common path mistakes such as writing transform vectors into `joint.limits`. Live gpt-5.5 headless verification after the change: electric fan prompt reached ready in 1 attempt with 0 failures/0 warnings and visual GLB inspection showed connected head/guard/blades; Golden Gate bridge prompt reached ready in 2 attempts with 0 failures/7 warnings and visual GLB inspection showed a coherent suspension bridge.
2026-05-31: Agent progress timeline rendering now consumes an engine-created progress snapshot instead of recombining raw agent events with a shared app-level candidate history. Each app run owns its own `CandidateHistory`, and `runManifestAgentLoop` emits `onProgress` snapshots containing upserted events, the matching history snapshot, and projected timeline items. The timeline projector also refuses to append unmatched validation attempts while any step is still running, preventing stale `Candidate validation failed` rows from appearing under the current repair spinner.
2026-06-01: Path tracer accumulation now presents raw `WebGLPathTracer` samples directly during progressive rendering and keeps asset bloom, EffectComposer output, and denoising out of the per-sample loop. Final post-processing runs once after the selected max sample target is reached; toggling denoise or changing the target marks that final output dirty. Scene upload uses `setSceneAsync()` with `three-mesh-bvh`'s `GenerateMeshBVHWorker`; do not start concurrent uploads, and keep later scene changes queued behind the in-flight worker task.
2026-06-02: Dynamic GLB export samples wrapped continuous controls based on the fastest bound joint scale. A grouped rotor control with main scale 1 and tail scale 4 previously exported both channels, but the tail keyframes landed only on full-turn quaternions and appeared static. Keep wrapped-control export sampling dense enough that high-scale linked joints have intermediate quaternion motion.
2026-06-02: Dynamic GLB export now emits one asset-level animation clip for all joint controls and material emission channels because common viewers may autoplay only the first clip. Non-wrapped joint controls use distance-proportional timing and the shared in-app joint animation speed helper, so exported oscillating controls no longer move faster in one direction than the other.
2026-06-02: Single-clip dynamic GLB export must still preserve independent control cycles. Do not stretch every joint control to the asset clip duration; repeat each control's own cycle inside the clip. Large revolute swings also need intermediate quaternion keyframes by angular travel, otherwise a `+pi` to `-pi` segment looks like a long pause because glTF quaternion interpolation takes the shortest equivalent rotation.
2026-06-02: Animated `connectorTube` GLB export should not depend on morph-target weight animation. Some viewers leave the base connector visible even though weight channels and morph targets are present. Dynamic export now replaces moving connector tubes with segmented cylinder meshes animated by standard TRS tracks baked at 30 Hz plus authored keyframes; when connectors are present, joint tracks use the same baked timeline so moving endpoints and connector segments stay phase-aligned.
2026-06-02: Provider model settings are now per-provider cached UI preferences. Defaults come from `modelConfig.ts` (`modelConfig.model`/`reasoningEffort` for OpenAI, `geminiModelConfig.model`/`thinkingLevel` for Gemini), while the shared UI label remains "Reasoning Effort"; OpenAI transport maps it to `reasoning.effort` (`none`/`low`/`medium`/`high`/`xhigh`) and Gemini maps it to `thinkingConfig.thinkingLevel` (`minimal`/`low`/`medium`/`high`). API keys remain local-env/session-only and are not persisted.

2026-06-03: Jet-engine cutaway run exposed false overlap pressure from open/cutaway `lathe` shells: hollow case surfaces were using solid segmented bounds, so valid internals inside a sleeve/case could be reported as `engine-frame` vs `rotor-spool` overlap and drive allowance/structure repair oscillation. `relationMetrics` now uses surface patch proxies for open/cutaway lathe visuals while preserving solid-style proxies for closed full-sweep profiles that touch the axis at both ends. Prompt guidance also now calls out visible seated blade roots for fans/compressors/turbines/propellers so repairs do not satisfy validation with barely tangent blade faces.
2026-06-03: The same jet-engine headless verification surfaced a repair-patch schema loop where the model wrote `part_exists`, `joint_exists`, or `expect_*` check objects into `/parts/.../visuals/.../geometry`. Patch-application summaries now include object `type=...`, and path hints explicitly route authored checks to `/checks` while requiring visual `geometry` to stay a valid primitive descriptor.
2026-06-03: A follow-up OpenAI repair run still repeated one `part_exists`-into-geometry patch and then exhausted `max_output_tokens`. Repair prompts now use a patch-only example instead of the full asset example, warn before any validation error that patch values must match their schema path, and forbid ordinary root replacement/full-asset output in repair mode.
2026-06-03: The in-app jet-engine failure reproduced the same engine-case/rotor-spool overlap plus schema-invalid repair alternation even with GPT-5.5/high. Headless had been using the same `src` agent loop and validator; the earlier ready result was a stochastic recovery, not proof the harness was fixed. Patch-application feedback now carries `<repair_target_validation_context>` so invalid repair patches do not erase the original validation target. Relation metrics now use mesh BVH narrow-phase checks after proxy broad-phase: true surface intersections and closed-mesh containment drive overlap decisions, exact mesh distance drives contact/support, and proxy depth/volume remain reporting approximations only. Mechanically critical same-part islands are blocking, and movable joints need a close visible parent/child mechanical fit so cutaway engines and similar CAD-like assets do not pass with loose motion groups.
2026-06-03: Exact jet-engine headless replay showed a staged-feedback friction: attempt 1 only reported missing `expect_material_side` checks even though the subsequent candidate exposed the same engine-frame/rotor-spool physical overlap after a side-check-only patch. `surface_side_missing_check` remains a blocking validation failure, but it no longer skips build/QC/checks/sampled/export stages, and repair feedback ranks it behind physical relation failures when both are present. This prevents renderer-side hygiene issues from hiding the mechanical failure that the next repair should actually address.
2026-06-03: Combustion-engine replay exposed a mesh-relation performance regression on a large patched candidate: exact BVH distance was being paid too broadly after a simple side-check repair. Mesh relation shapes are now built lazily per queried visual pair, and exact distance refinement is gated to overlapping or near-threshold broad-phase pairs. Exact mesh intersection/containment still decides overlaps, but far proxy pairs keep their broad-phase distance because they cannot affect contact, support, or movable-fit thresholds.
2026-06-03: Follow-up validation profiling on the exact jet and combustion candidates showed sampled-pose QC was repeating current/rest overlap pairs and static relative-transform pairs, and pure overlap scans were paying exact closest-distance costs they did not need. Baseline QC now shares one mesh relation index per built rest asset, overlap scans use intersection-only mesh refinement, sampled-pose overlap QC skips unchanged part pairs and visual pairs already failing in current pose, and repair feedback receives fewer duplicate sampled-pose failures while still catching new articulated collisions.
2026-06-03: CAD-like mechanical quality pass should stay general, not prompt-specific. Mechanical prompts now require named path/coupler components when requested, `expect_path_contacts` for routed belts/chains/tracks/cables/hoses/ropes/straps/wires, exact visual-scoped relation evidence for multi-visual rods/linkages, and fitted-interface checks for guided/rotary parts. Separate one-off `expect_contact` checks do not count as wrapped path coverage.
2026-06-03: Saved/deposited assets should not be fully revalidated just to view from the asset panel. Use the persisted validation report for selected saved versions; revalidation belongs to creation/editing/export readiness, not library selection.
2026-06-03: Large mechanical candidates exposed relation-validation cost from rebuilding proxies and exact closest-distance queries too broadly. Reuse relation proxy sets per built pose, cap open-lathe broad-phase proxy segments independently from render segments, use intersection-only mesh refinement for overlap/support predicates where exact distance is not needed, and reserve closest mesh distance for authored contact checks or failure diagnostics.
2026-06-03: CAD-like mechanical component coverage should be based on named part identity, not visual-only labels. Visual ids are useful for exact `expect_*` refs, but prompt-critical components like belts, chains, rods, pistons, crankshafts, pulleys, sprockets, gears, and shafts should exist as named parts first; wrapped or wound connector-like paths still need `expect_path_contacts`.
2026-06-03: Mechanical path classification must not treat support names/descriptions like "belt pulley", "chain sprocket", or "shaft bearing" as the routed path itself. Use part id/name/role for component identity, ignore descriptions for path/coupler/guided/rotary presence, and classify wheel/rotary supports separately from belts/chains/cables.
2026-06-03: `HEADLESS_AGENT_REPEATED_FAILURE_STOP_STREAK=0` is meant to disable the diagnostic repeated-failure stop. The headless env reader must allow zero for that setting even though positive-only parsing remains appropriate for timeouts and repair-turn caps.
2026-06-03: CAD-like mechanical relation coverage now checks target quality, not just authored relation count. Wrapped paths that mention pulleys/sprockets/wheels/gears must target those rotary supports; rods/linkages in guided+rotary prompts must prove both endpoint categories; pistons/sliders/valves need guide/liner/cylinder/rail/housing/support evidence; linked mechanical prompts need at least one multi-joint control when multiple movable joints exist.
2026-06-03: Patch-application path hints for invalid visual `geometry.type` must fire from the schema error path alone, not only from rejected-operation summaries. Compact summaries can hide the bad operation behind "...and N more", so geometry-domain guidance should still tell the model to use a valid primitive descriptor or patch authored checks under `/checks`.
2026-06-04: Keep CAD-like mechanical relation coverage isolated in `src/engine/validation/validateMechanicalRelations.ts`, with shared relation-pair extraction in `relationCheckPairs.ts`. `validateStructure.ts` should remain the structural orchestrator; future mechanical target-quality changes belong in the dedicated module rather than growing the main validator.
2026-06-04: Rest-pose relation evidence is not enough for linked CAD-like mechanisms. When prompts imply coupled moving parts through crank, timing, belt/chain-driven, gear-train, drivetrain, or linkage language and the asset has multiple movable joints, mechanical relation coverage now requires pose-specific `check.pose` evidence for the fitted interfaces that must remain coupled through motion: rod/linkage endpoints, guided movers in guides, and routed paths on their supports.
2026-06-04: Global overlap QC should not force visually plausible fitted mechanical contacts apart. Exact visual-pair `expect_contact`, `expect_path_contacts`, or `expect_gap` checks with bounded `maxPenetration` now classify the same current/sampled-pose overlap as a fitted-contact note when the measured penetration stays within the bound. Use `allow_overlap` for intentional exceptions that are not bounded fitted contacts; stale, broad, or over-penetrating checks still fail.
2026-06-04: Do not salvage presence-only `part_exists` or `joint_exists` descriptors from `/parts/.../visuals/.../geometry` repair paths. A live CAD-like piston-pump repair showed that check-only geometry patches can become schema-valid while leaving the physical overlap/fit failure unchanged and polluting candidate history. Visual `geometry` patches must repair geometry; presence checks belong under `/checks` and are rejected with path hints when placed into geometry.
2026-06-04: Do not salvage `allow_overlap`, `allow_isolated_part`, or other allowance descriptors from visual `geometry` paths. If a repair patch puts an allowance in `/parts/.../visuals/.../geometry`, reject the patch and route the next attempt to `/allowances/-`; silently rerouting lets the model keep writing descriptors in the wrong schema domain.
2026-06-04: Visual-geometry patch-domain rejection should catch obvious placeholder references such as `x`, `y`, `a`, `b`, `dummy`, `example`, `__invalid__`, `part-a`, or `visual-b` in the rejected summary/path hint. Leaving these as patch-application errors prevents impossible `/checks/-` or `/allowances/-` entries from polluting candidate history and wasting later turns on reference failures.
2026-06-04: Presence-only `part_exists` and `joint_exists` checks are especially prone to no-op physical repairs, but the same strict visual-geometry rule now applies to all authored checks. Patch feedback should preserve the original validation target context and tell the model that a check-only patch is not a geometry repair for contact, overlap, fit, support, or motion failures.
2026-06-04: CAD-like guided motion now distinguishes visual relation evidence from kinematic plausibility. If a prompt asks pistons/sliders/plungers/sleeves/valves to slide, stroke, reciprocate, or participate in a crank/linkage mechanism, mechanical relation coverage requires a prismatic joint from the guide/cylinder/rail/housing/support to the guided part, and linked guided mechanisms need one control binding that prismatic joint with a rotary joint. Rod/contact checks prove visual coupling; they should not replace the guided mover motion joint.
2026-06-04: Pose-specific authored checks use joint poses, not transforms. When a repair patch creates `check.pose` with `position`/`rotation`/`scale`, patch feedback now adds a targeted hint to use `pose.joints[{ jointId, value }]` and to patch visual transforms or joint origins separately.
2026-06-04: `check.pose.joints` entries are joint value samples, not check descriptors. If a repair patch writes `{ type: "joint_exists", jointType: ... }` inside `pose.joints`, patch feedback should tell the model to use `{ jointId, value }` there and put `joint_exists` as a separate check under `/checks/-`.
2026-06-04: Repair patches can also accidentally paste an asset-shaped object into `check.pose`. Patch feedback should explicitly reject `schemaVersion`, `parts`, `materials`, `checks`, `allowances`, and `metadata` inside `pose`; a pose is only `{ name, joints: [{ jointId, value }] }`.
2026-06-04: Repair patches can nest a whole authored check descriptor inside `check.pose` (`type`, `partAId`, `partBId`, `visualAId`, etc.). Patch feedback should call this out separately from transform-shaped pose mistakes: additional checks belong under `/checks/-`, while `pose` only contains sampled joint values.
2026-06-04: Repair patches can also put authored check descriptors into `controls/*/limits`, or full joint descriptors into `check.pose.joints`. Patch feedback should route checks back to `/checks/-`, remind that control limits are only numeric `lower`/`upper`, and remind that pose joint samples are only `{ jointId, value }` while full joint definitions belong under `/joints`.
2026-06-04: Mechanical relation-coverage failures should not skip downstream validation. Missing CAD-like evidence is blocking for acceptance, but build/QC/checks/sampled/export should still run so first-attempt feedback includes both missing relation evidence and physical overlap/support defects, avoiding staged repair churn where a check-only patch reveals collisions one turn later.
2026-06-04: Generated sampled-pose validation now samples intermediate phases for multi-joint non-wrapping controls. This is important for CAD-like linked mechanisms such as crank/rod/piston, belt, chain, linkage, drivetrain, and gear-train controls: end stops alone can miss the quarter-cycle or half-cycle pose where rods detach, collide, or paths leave supports.
2026-06-04: CAD-like mechanical motion coverage must validate kinematic authorship, not only fitted contact evidence. Prompt-critical rotary movers such as crankshafts, shafts, gears, pulleys, sprockets, wheels, rotors, turbines, fans, and impellers now need a revolute/continuous support joint when the prompt asks for rotation, drive, timing, or coupled motion; linked mechanical prompts with multiple movable joints must still fail `mechanical_linked_control_missing` even if the model omits the `controls` array entirely.
2026-06-04: Mechanical relation-coverage failures should not dominate repair feedback when downstream physical defects are already available. Keep them blocking for acceptance, but sort them after real overlap, authored contact/path failures, and sampled-pose failures so repairs fix the mechanism geometry/fit first instead of adding more check evidence while the asset is still visibly colliding or detached.
2026-06-04: Patch-application recovery must preserve the same failure priority as normal repair feedback. When a repair patch is schema-invalid, `<repair_target_validation_context>` should order the previous candidate's primary failures with physical overlap/contact/sampled-pose defects ahead of mechanical relation-coverage evidence, otherwise one bad patch can push the model back toward check-only evidence while the real fit/collision problem remains.
2026-06-04: Repair patches are all-or-nothing. Patch-application feedback should explicitly say that no operation from a rejected patch was partially applied and that useful valid operations must be resent with the bad operation corrected. This matters for long mechanical repair patches where one misplaced check in a visual `geometry` path can otherwise make the model assume the later `/checks/-` or `/allowances/-` additions stuck.
2026-06-04: Repair response rules should follow the dominant repeated physical failure cluster when a singleton floating/detail issue would otherwise be listed first. Keep schema/build/root/model-validity blockers first, but for CAD-like mechanical runs a many-count overlap, path-contact, exact-gap, mechanical-fit, or sampled-pose cluster is usually the mechanism-level repair target; chasing one detached detail first can waste the next repair turn while the major fit problem remains.

2026-06-04: CAD-like containment fits need the same bounded-overlap path as seated contacts. Exact visual-scoped `expect_within.maxPenetration` now proves guided/captured containment for current and sampled poses, so validators do not force pistons, sliders, plungers, or shafts out of liners, rails, sleeves, or bearings just to clear global overlap QC. Prompt-critical path supports such as sprockets, pulleys, gears, rollers, wheels, rims, hubs, bearings, and collars should be named parts, not only visuals inside a broader shaft or housing, because relation coverage is part-identity based.

2026-06-04: Invalid repair-patch feedback now lists hidden schema-domain operations when the compact patch summary omits them. This is important for long CAD-like repair patches where one late operation writes a check or other non-primitive object into `/parts/.../visuals/.../geometry`; the model needs the concrete hidden operation path/value to resend the useful valid operations without repeating the schema mistake.
2026-06-04: Relation-loop hints need actual cross-attempt evidence. Do not show "recent repairs alternated" just because one candidate reports both overlap and contact/path-gap failures for the same pair; require too-close and too-far states to appear in distinct failed revisions.

2026-06-04: CAD-like cutaway housings should be modeled as open clearance geometry around mechanisms, not broad solid covers that collide with internals and depend on repair allowances. Prompt and repair feedback now tell the model to split static blocks, heads, covers, guards, frames, rails, walls, cases, and shells into windows, posts, collars, brackets, flanges, panels, or open shells around moving swept volumes before moving the mechanism loose or adding allowances. `maxPenetration: 0` is only for true surface touch; captured liners, bearings, collars, sleeves, rails, slots, and seated wrapped paths should use small positive bounded fit proof.

2026-06-04: Patch-application feedback should surface the exact failed operation when an error says "Patch operation N failed", even if compact patch summaries would normally omit it. This matters for brittle numeric paths such as `/checks/43/maxPenetration`: the model needs the failed operation plus the append/by-id hint to stop guessing array indexes and resend useful operations against the still-unmodified candidate.

2026-06-04: Misplaced visual-geometry authored checks should be rejected, not salvaged. If a repair patch writes `expect_contact`, `expect_path_contacts`, `expect_gap`, `expect_overlap`, `expect_within`, `expect_material_side`, `part_exists`, or `joint_exists` into a visual `geometry` path, emit a patch-application error with `/checks/-` guidance; otherwise the model never gets correction for the wrong path and repeats the schema-domain mistake.

2026-06-04: CAD-like couplers should not be represented as connectorTube-only parts. `connectorTube` is appropriate for flexible endpoint-routed cables/hoses/ropes/straps/tethers/wires, but connecting rods, pushrods, tie rods, link arms, and crank linkages need rigid local geometry with bearing-eye/pin/socket/clevis-style ends plus exact endpoint checks. Mechanical relation coverage now fails coupler parts whose only visuals are connectorTubes.

2026-06-04: CAD-like linked couplers now require motion participation, not just rigid shape and rest-pose contact. In linked guided-to-rotary mechanisms, rods/linkages with only fixed joints fail mechanical relation coverage; they need at least one movable pivot joint at a plausible pin/bearing/clevis/socket/wrist-pin/crank-pin/journal endpoint, and that pivot should be included in the same linked control as the guided and rotary joints.

2026-06-04: Repair patch normalization now treats `replace` at an append path ending in `/-` as `add`, because model repair patches often use replace when appending checks or control bindings. Keep schema validation after normalization. Patch feedback also now explains that `/controls/.../joints` entries are control bindings `{ jointId, scale, offset }`, not `joint_exists` checks or full joint definitions; joint checks belong under `/checks/-`, and joint definitions under `/joints/byId/...`.

2026-06-04: Combustion-engine headless artifacts showed the general CAD-like friction pattern: the first candidate used good mechanical vocabulary but overbuilt broad static supports/enclosures, then repair turns alternated between large physical-overlap sets and schema-invalid no-op patches (`part_exists`/`allow_overlap` into visual geometry). Repair feedback now groups repeated mechanical relation-coverage failures into a compact mechanism contract summary, classifies inserted-support fits such as liners/bearings/shafts/hubs in blocks/housings separately from generic swept-volume clearance, and places patch path hints before rejected patch summaries so schema-domain repair mistakes lead the next turn.

2026-06-04: Superseded: headless briefly had an explicit OpenRouter-only adapter beside `agentPipelineSmoke.test.ts`. OpenRouter is now a first-class app provider and headless must use `createManifestProviderClient` for `HEADLESS_AGENT_PROVIDER=openrouter` rather than a separate test-only client.

2026-06-04: OpenRouter headless transport should retry transient network and upstream HTTP failures. A combustion-engine OpenRouter run produced a valid create response and a correctly rejected `part_exists`-into-visual-geometry repair patch, then stopped only because the next repair request returned an immediate `fetch failed`. Keep the OpenRouter adapter retrying short network/transient failures so headless evidence is not lost to one transport blip.

2026-06-04: Control `joints` arrays are a common repair-patch schema-domain trap: they accept only control bindings `{ jointId, scale, offset }`, while authored `part_exists`, `joint_exists`, `expect_*`, and `allow_*` descriptors belong under `/checks/-` or `/allowances/-`. Patch-application feedback should flag hidden invalid control-binding operations, especially in long repair patches where a later `remove /controls/.../joints/-` failure can otherwise hide the earlier bad `add` descriptors. For cost-sensitive repair debugging, `HEADLESS_AGENT_RUN_MODE=initial` runs one create attempt with zero repairs, while `HEADLESS_AGENT_RUN_MODE=repair HEADLESS_AGENT_REPAIR_FROM_CANDIDATE_PATH=.../attempts/NN/candidate.json` seeds the first exchange from a previous candidate and sends exactly one live repair request. Use these targeted modes to confirm repair-feedback fixes before paying for another full create+repair run.

2026-06-04: OpenRouter repair replay from a saved combustion-engine candidate isolated a first-repair contract failure without another create request: the model returned a template-like `allow_overlap` object with `dummy` refs inside a visual `geometry` path. Keep repair prompts and response contracts explicit that visual `geometry` accepts only primitive geometry descriptors, checks belong under `/checks`, allowances under `/allowances`, and template/dummy/example/todo placeholder ids are invalid before the loop spends a turn rejecting them.

2026-06-04: A follow-up OpenRouter repair replay showed prompt hardening alone is not enough if the harness silently salvages schema-domain mistakes. The patch moved useful geometry but still wrote `allow_overlap` into a visual `geometry` path; because it had concrete ids, the previous normalizer rerouted it to `/allowances/-` and gave the model no correction. The agent loop now rejects checks and allowances placed in visual geometry so the next repair prompt includes path hints and the rejected patch summary.

2026-06-04: Full text-only combustion-engine OpenRouter run stopped after two consecutive schema-invalid repair patches. First repair wrote an `expect_overlap` object with placeholder refs into visual geometry; second pasted a complete Manifest3D asset into `/joints/5/limits`. Patch feedback already rejected both, but the whole-asset operation was hidden behind compact summaries, so hidden schema-domain summaries now also flag nested whole-asset patch values. Repair contract text now explicitly forbids complete asset objects as nested patch values.

2026-06-04: Headless has a diagnostic patch-application stop in addition to repeated validation-signature stop. By default `HEADLESS_AGENT_PATCH_ERROR_STOP_STREAK=2`, so a full headless run returns a failed result before sending another live repair request after two consecutive schema-invalid repair patches. Disable with `HEADLESS_AGENT_PATCH_ERROR_STOP_STREAK=0` only when intentionally observing the full app repair cap.

2026-06-04: Repair structured-output schema now separates generic patch values from `/checks` and `/allowances` patch values. Generic nested patch operations can no longer return complete Manifest3D assets, authored check objects, or allowance objects; check values are only valid on `/checks...`, and allowance values only on `/allowances...`. This blocks model outputs like `replace /parts/.../geometry value={ type: "part_exists", partId: "dummy" }` at structured-output time instead of relying only on prompt text and patch validation.

2026-06-04: OpenRouter transient retry must cover response-body read failures as well as initial fetch failures. A seeded repair replay hit Undici `TypeError: terminated` / `UND_ERR_SOCKET` while reading the response body; treat that as `network_error` so the same retry path handles it.

2026-06-04: Agent generation now uses a normalized session/tool-call harness instead of provider-specific repair response schemas. Providers return a compact `{ tool, argumentsJson }` envelope: `submit_manifest_asset` for initial creation and `apply_manifest_patch` for edit/repair, where patch add/replace values are encoded as `valueJson` and parsed by the shared harness before JSON Patch application and full Manifest3D validation. Persisted app sessions keep only replay-critical exchanges, provider response ids, token estimates, and continuation links; headless-only request metrics and artifact paths remain diagnostic artifacts only. OpenRouter is now a first-class app provider via `src/engine/agent/openRouterManifestClient.ts`, so headless `HEADLESS_AGENT_PROVIDER=openrouter` must route through `createManifestProviderClient`, not a test-only adapter. Gemini now uses the Interactions API with `previous_interaction_id` for same-session continuation where available; OpenAI uses Responses `previous_response_id`.

2026-06-04: OpenRouter does not provide the same server-side continuation path as OpenAI Responses or Gemini Interactions. Persist OpenRouter response ids for traceability, but do not use them to omit current-candidate JSON or send `previous_response_id` in repairs/edits; the harness supplies candidate context client-side. Repair patch tools should reject root path `""` and wrapper paths like `/asset` or `/assets`; the canonical candidate JSON is already the patch document root, and whole-asset replacement reintroduces the monolithic-output failure mode this refactor is meant to avoid.

2026-06-05: Supersedes the 2026-06-04 compact `{ tool, argumentsJson }` live contract. The outer `argumentsJson` string made initial creation and repair/edit turns double-encode JSON, which caused schema-invalid initial assets and malformed inner JSON strings in live app/headless runs. Live create turns now use the full Manifest3D asset schema directly and return the asset as the response root. Live edit/repair turns now use the shared compact patch tool object `{ tool: "apply_manifest_patch", operations: [{ op, path, valueJson }] }`; only individual add/replace values remain stringified as `valueJson`. Keep legacy `argumentsJson` parsing for old fixtures/persisted diagnostics, but do not prompt or structure live provider responses around it.
2026-06-05: OpenRouter app/headless provider now uses `/api/v1/chat/completions` instead of `/api/v1/responses`. The Responses-shaped payload (`input`, `instructions`, `text.format`, `max_output_tokens`, `session_id`) is too OpenAI-specific and produced immediate 400s for cross-vendor models such as `anthropic/claude-opus-4.8`. OpenRouter requests should send normalized chat `messages`, `max_tokens`, `provider.require_parameters: true`, and `reasoning.exclude: true`; default OpenAI-family models can use strict Manifest3D JSON schema, while non-OpenAI create turns may need `response_format: { type: "json_object" }` because Anthropic rejected the full asset schema as an overlarge compiled grammar. Keep canonical Manifest3D validation as the real enforcement layer for any provider response schema relaxation. Provider raw error metadata should be surfaced in timeline messages so 400s are diagnosable from the app.
2026-06-05: OpenRouter provider profiles are deliberately not one-size-fits-all. All OpenRouter calls use app attribution headers (`HTTP-Referer=https://manifest3d.scottsun.io`, `X-OpenRouter-Title=Manifest3D`), `provider.sort.by=throughput`, and top-level `session_id` for sticky routing/cache locality. Anthropic uses explicit `cache_control` on the system prompt block and JSON-object create mode because the full Manifest3D schema exceeded Claude Opus 4.8's grammar compiler. OpenAI-family OpenRouter models keep strict full-schema create output. General routed models use JSON-object mode and canonical validation afterward. MiniMax M3 needs explicit image detail (`high`, not `auto`) and reasoning disabled; otherwise it can return HTTP 200 with `message.content=null` after spending the completion on reasoning.

2026-06-05: Contract/schema prompt wording matters in replay sessions. `schema.md` must describe the canonical asset document without telling repair/edit turns to return a full asset; the mode-specific response contract is authoritative. If future harness changes add another transport wrapper, make sure create/edit/repair prompt examples, provider response schemas, and `parseManifestAgentToolCall` all expose the same shape before running expensive headless tests.
