2026-05-16: Phase 1 UI should use `refs/UI_ref.png` as the visual target: Manifest3D title anchored left inside top translucent chrome, dominant open WebGPU viewport, right frosted chat/agent panel. No dev server per project AGENTS rule; verify with test/typecheck/lint/build instead.
2026-05-16: Strict UI correction from user: keep phase 1 viewport empty, no mock assets/messages/timeline/status chips/toolbars, no visible viewport container, no grid. Use a plain diffuse white ground and a fixed-corner 3D XYZ gizmo rendered in WebGPU, not a CSS overlay.
2026-05-16: Browser verification screenshots for UI iteration belong under `dev_docs/ui_verification/`, not the project root.
2026-05-16: UI shell should be flattened: WebGPU canvas is full-screen app background, while top chrome and right chat panel are absolute overlays. Avoid a visible workspace/grid container or panel inner-border nesting.
2026-05-16: User does not want browser verification unless explicitly requested. The XYZ gizmo should be Blender-style with high-resolution canvas sprite markers, padded render viewport, and it should animate from open-viewport top-right to full-window top-right when the side panel collapses.
2026-05-16: Approved gizmo direction changed to React Three Drei built-ins: use `GizmoHelper` + `GizmoViewport` for the XYZ gizmo, not custom geometry or CSS overlays. The side-panel collapse state should drive the gizmo margin so it slides between open viewport and full viewport top-right positions.
2026-05-16: Logo direction uses `public/logo.png` plus a responsive glassy SVG wordmark adapted from `refs/logo_demo.html`; keep the top chrome sparse and do not add right-side nav tags or viewport chips.
2026-05-16: Desktop side panel right edge should align with top chrome right edge. Keep the Drei gizmo low enough that its markers do not encroach on the top bar.
2026-05-16: Side panel desktop width should be responsive within min/max bounds, not fixed. To keep the Drei gizmo crisp, use default `GizmoViewport` sizing instead of scaling up `scale`, `axisHeadScale`, or custom font sizing; Voxelit uses this default-size pattern.
2026-05-16: User changed the prompt placeholder copy to `What to create?`; preserve user copy changes unless explicitly asked to alter them. Send icon needs a small optical centering nudge inside its circular button.
2026-05-16: To avoid blurry gizmo labels under the viewport DPR cap, render the Drei `GizmoHelper`/`GizmoViewport` inside a small transparent WebGPU overlay canvas at normal device density and sync it from the main scene camera quaternion. This keeps the main viewport DPR budget intact while making the built-in Drei sprites crisp.
2026-05-16: Desktop side panel bottom inset should match the top chrome top inset so outer UI margins feel consistent.
2026-05-16: The temporary phase-2 `demoManifestScene` runtime fixture was removed after user approval. App startup should use an empty Manifest3D scene until real agent-generated assets are wired in. Export toolbar visibility is no longer selection-gated; it stays in the top chrome and is disabled when no asset is selected.
2026-05-16: The main WebGPU canvas and the separate Drei gizmo overlay now use R3F `frameloop="demand"` so idle scenes stop rendering. When adding animated camera/scene behavior, explicitly call Fiber `invalidate()` while motion is active, as the selection snap loop does.
2026-05-16: Effective viewport centering should treat the visible viewport right boundary as the side panel's left edge. For Three `PerspectiveCamera.setViewOffset`, use a positive `offsetX` equal to half the right occlusion width to move the orbit target left into the visible viewport center.
2026-05-16: Historical V0 validation harness staged candidates as schema -> semantic -> build -> geometry -> prompt tests, then optional commit. Treat this as provisional scaffolding only; the revised plan supersedes it with Contract V2, signal bundles, baseline QC, exact checks, allowances, repair feedback, and freshness.
2026-05-16: `dev_docs/init_plan.md` was rewritten after Articraft audit. UI/renderer are treated as the stable base; schema, validation, prompt tests, agent loop, and export are provisional unless they align with Articraft-style Contract V2, signal bundles, baseline QC, exact checks, allowances, repair feedback, and candidate freshness.
2026-05-16: Phase 3 core has been rebuilt around Contract V2: assets now carry stable ids, units, checks, and allowances; parts no longer use `parentId`; the builder assembles via joints; validation emits signal bundles with structural checks, baseline QC, exact checks, overlap/isolation allowances, and export readiness. `src/engine/examples/rendererMockAssets.ts` is a dev fixture and should remain unplugged from app startup by default.
2026-05-16: Phase 4 agent infrastructure is local/pure TS: `candidateHistory.ts` gates readiness by active candidate fingerprint matching the latest successful validation attempt; repeated failure signatures are derived from failure signals, not whole reports. `repairFeedback.ts` renders model-facing `<validation_signals>` from bundles and prioritizes schema/structure/build before QC/check repair. Prompt source lives in `src/engine/agent/prompts/*.md` and is imported by `promptCompiler.ts`.
2026-05-16: Manifest3D prompts were compared against Articraft `agent/prompts/sections/designer_common.md`, `link_naming.md`, and `sdk_base.md`. Local prompts now carry the relevant non-Python guidance: realistic geometry as the dominant quality bar, real-world scale/materials, primary mechanism articulation, no floating parts, scoped intentional overlaps paired with exact proof checks, stable semantic ids/names, and "validation as sensors, not optimization targets."
2026-05-16: Phase 5 uses `src/engine/config/modelConfig.ts` as the only model settings source and keeps Responses API details isolated in `src/engine/agent/openAiManifestClient.ts`. The OpenAI client sends `text.format.type=json_schema` with strict Manifest3D structured output plus `input_image` payloads; missing `VITE_OPENAI_API_KEY` returns an unavailable result without changing the scene. `agentLoop.ts` records invalid attempts in `candidateHistory`, renders repair feedback for retries, and commits only a fresh valid candidate through `sceneStore.upsertAsset`.
2026-05-17: Persistence now separates the saved asset library from runtime viewports. `assetLibraryStore` persists asset records, version records, and per-version validation attempts to IndexedDB; assets are ordered by original creation time descending, not last update. `sceneStore` is runtime-only: Create has one `create` instance, Compose has many transformed instances. Edit prompts for saved versions include a compact validation-attempt context, not LLM transcripts.
2026-05-17: Phase 5 design choices are captured in `dev_docs/phase5_design.md`. Key choices: no persisted LLM transcripts, persist validated asset versions plus per-version attempts, keep Create single-asset and Compose multi-asset/runtime-only, normalize validation timeline messages, and make Compose transform constraints reject below-table scale/rotation rather than auto-lifting.
2026-05-17: Phase 6 GLB export lives in `src/engine/scene/exportGlb.ts`. The top-chrome Export GLB button remains visible but is enabled only when the Create workspace has a viewed `create` instance loaded; it does not depend on the transient renderer outline selection. Compose selections intentionally do not export. Export builds the viewed Create manifest asset into an isolated group, clones it, strips helpers/non-exportable objects/userData, converts WebGPU node materials to standard GLTF-friendly materials, and downloads a binary `.glb`.
2026-05-17: GLB export regression fix: `MeshStandardNodeMaterial` can also present `isMeshStandardMaterial`, so export conversion must explicitly reject node materials before cloning. Otherwise GLTFExporter writes material slots but loses authored color/metalness/roughness and falls back to default PBR values.
2026-05-17: Selection emphasis should not tint asset materials. Use Three's WebGPU/TSL `OutlineNode` pipeline, not per-mesh back-side expansion, so selected assets render as one screen-space mask with a camera-perspective silhouette rather than internal component outlines.
2026-05-17: OpenAI API keys are startup-gated by browser hostname: `VITE_OPENAI_API_KEY` is used only on `localhost`, `127.0.0.1`, or loopback IPv6. Other origins must use the in-app API Key modal, which stores the provided key only in memory for the current browser session.
2026-05-17: When tuning selection outlines, keep the `OutlineNode` edge mask boosted with `edgeGlow`, then use that mask to replace pixels with the outline color. Pure additive composition disappears over the light table plane because bright backgrounds clamp toward white.
2026-05-17: User-approved main viewport DPR tuning now uses `maxPixels = 4_000_000` and a DPR cap of `1.75`; keep renderer tests aligned with those values.
2026-05-17: Selection camera centering must react to the selected Three object becoming available, not only to `selectionRevision`. History opens and fresh Create results can set selection before the asset group registers, so `SelectionCameraTarget` depends on the resolved selected object.
2026-05-18: Engine/harness audit follow-up tightened the OpenAI structured-output schema with vector lengths, positive dimensions, segment ranges, nonnegative check tolerances, material bounds, and minimum generated part/material/visual arrays; keep prompt examples parseable against Contract V2 because they materially steer headless candidate shape.
2026-05-18: Two full live `npm run test:headless` attempts reached only the OpenAI fetch layer and failed after about 301s with `fetch failed`, so they are not asset-quality evidence. `.env` had an OpenAI key entry and the shared app OpenAI client was left unchanged. A shortened `HEADLESS_AGENT_EXPECT_READY=0 HEADLESS_AGENT_RUN_TIMEOUT_MS=5000 HEADLESS_AGENT_TIMEOUT_MS=65000 npm run test:headless` run passed and now records per-exchange duration; use that when diagnosing API reachability separately from validation quality.
2026-05-18: Headless test now uses a headless-only Node `https` fetcher with `HEADLESS_AGENT_FETCH_TIMEOUT_MS` so long Responses calls are not cut off at Node fetch's observed ~301s boundary. The app OpenAI client remains unchanged.
2026-05-18: One-hour headless stress runs passed with real model outputs: toolbox prompt reached ready in 2 attempts after sampled-pose overlap repair; pickup prompt reached ready in 4 attempts after 128/42/40 overlap failures. Main harness pain point was repair-feedback bloat from repeated visual-pair overlap signals (pickup repair prompts were 78k-110k chars), so `repairFeedback` now compacts repeated signal groups for model-facing feedback while keeping the full signal bundle/artifacts intact.
2026-05-18: Headless summaries now record run duration, exchange count, per-exchange duration, prompt character sections, and rough char/4 token estimates. The live prompt matrix covered simple/static chair (ready in 1 attempt, ~2.6k input/~1.6k output tokens), simple/articulated toolbox (ready in 2 attempts, repair ~11.2k input/~3.0k output), difficult/static castle gatehouse (ready in 2 attempts, repair ~30.6k input/~8.6k output), and difficult/articulated pickup (ready in 3 attempts after feedback compaction, repairs ~21.8k-21.9k input/~7.1k-7.6k output). Context is now much less wasteful in validation feedback, but full candidate JSON echo dominates repair turns, especially for high-detail static assets.
2026-05-18: Second harness pass keeps candidate-history freshness as the hard invariant and adds revision/fingerprint repair-context guidance to make that invariant visible to the model. Repair turns now include complete but minified candidate JSON, reducing candidate-context whitespace without dropping geometry; on existing stress artifacts this would shrink castle attempt-1 candidate context from 105,641 to 40,687 chars and pickup attempt-1 from 60,937 to 25,043 chars. App runtime uses the same one-hour run timeout as headless stress. Contract V2 now includes manifest `controls` so generated assets can group multiple joints under one UI dial or fall back to independent joint dials; the joint preview panel scrolls when controls overflow.
2026-05-18: Live post-controls headless schema check passed: simple static chair prompt reached ready in 1 attempt after 218.6s with ~2,953 input and ~1,972 output tokens. The returned static asset used fixed assembly joints and `controls: []`, confirming the updated strict schema is accepted by the API.
2026-05-18: Dynamic GLB export is now selected only for assets with movable joints. Static assets keep the direct Export GLB action; movable assets open a Static/Dynamic menu. Dynamic export generates standard glTF animation clips from manifest controls or fallback joint controls, targeting cloned joint groups with TRS tracks while still exporting the canonical rest asset.
2026-05-18: App startup is desktop-gated in `src/main.tsx`: mobile/tablet browsers receive only the desktop-only message inside a lightweight top frame with the logo image plus the same filtered SVG wordmark design as desktop, and do not import the React/WebGPU app bundle. Create/edit runs are task-scoped rather than global: multiple create jobs can show as top `Creating` rows, edit jobs attach to their source asset row, and opening a running row restores that task's prompt/progress until it finishes. AppShell runs jobs against isolated scene stores so browsing saved assets does not mutate in-flight prompt context; saved assets persist through per-asset IndexedDB writes instead of clearing the whole library snapshot.
2026-05-18: OpenAI API keys must never enter the built client bundle. Vite now uses `envPrefix: 'MANIFEST3D_PUBLIC_'` so `VITE_OPENAI_API_KEY` is not exposed to browser code, and local development reads `.env` keys through the dev-server-only `/__manifest3d/local-openai-api-key` endpoint restricted to localhost host headers. App code should not read `import.meta.env.VITE_OPENAI_API_KEY`.
2026-05-19: The top-bar API Key button now shows readiness with a status dot. Green means a usable key is loaded either from the localhost-only startup `.env` endpoint or from the in-memory session modal; red means no usable key is currently loaded, including localhost startup when `.env` is missing or blank.
2026-05-19: API Key toolbar availability is hostname-based, not readiness-based: localhost/loopback keeps the button visually active-looking but click-locked because local keys must come only from the dev-server `.env` endpoint. Deployed origins keep the button clickable for the in-memory modal. The status dot still reflects actual key readiness, and app code must not read API keys from `import.meta.env` or otherwise put them in the built bundle.

2026-05-19: In Create workspace, asset history selection now represents the viewed Create asset, not the transient renderer object-selection outline. Clearing the viewport outline should not clear the left-panel active asset state, edit-mode right panel, version navigation, or export eligibility while the Create viewport still shows that asset. Pending create runs use the active run id for the highlighted `Creating` row.
2026-05-20: Pipeline quality pass added Contract V2 `roundedBox` and `capsule` geometry primitives so generated assets can represent softened manufactured housings, panels, handles, rails, grips, and rounded pins without excessive box/cylinder stacks. Multi-joint articulated assets now fail validation unless manifest `controls` cover their movable joints; this is an app quality invariant for preview/dynamic GLB behavior, not a headless-test accommodation. Headless GLB artifact export remains self-contained in `test/headless/agentPipelineSmoke.test.ts`.
2026-05-20: Headless image-conditioned fan stress run exposed false-positive overlap pressure from hollow `torus`/closed `tube` visuals: the old broadphase treated a ring/grille AABB as a filled disk, causing repeated guard-vs-rotor failures. `overlapChecks` now splits torus and tube visuals into segment bounds before overlap testing, while still reporting real material intersections. Prompt guidance also calls out clearance for moving rotors/blades/wheels inside stationary guards, grilles, cages, and shrouds.
2026-05-20: Material emission is now an authored material property: materials can carry nullable `emission` and `emissionAnimation` fields, with keyframed on/off, color, intensity, loop, and step/linear interpolation. The renderer applies emission preview state per scene instance, the existing animation panel shows material emission timelines next to joint controls, and dynamic GLB export post-processes binary GLBs with real `KHR_animation_pointer` channels targeting material `emissiveFactor` and `KHR_materials_emissive_strength.emissiveStrength`; static export intentionally omits those animation channels.
2026-05-20: Generated sampled-pose validation now samples manifest preview controls rather than each movable joint in isolation. This better matches UI/export behavior for linked mechanisms such as paired steering or mirrored wheel spin and avoids impossible one-joint repair pressure. Structure validation also fails controls whose limits/bindings clamp to no effective joint motion, because those controls would otherwise look valid while preview/export cannot animate them.
2026-05-20: Pipeline quality pass made intentional overlap allowances enforceable: each `allow_overlap` now needs a matching authored proof check for the same part pair, and visual-scoped allowances need the same visual pair in that proof. Broad overlap allowances stay non-blocking but warn. Prompt/repair guidance now also calls out `roundedBox.radius <= min(size)/2` and asks generated visuals inside one part to touch/mount or be split into fixed child parts. Live headless checks: police-car material-emission prompt reached ready in 1 attempt/338.8s with empty signals and static+dynamic GLBs; articulated pickup reached ready in 3 attempts/820.4s with static+dynamic GLBs, down from the recent comparable 5-attempt/1529.3s run, though it still had one non-blocking disconnected-island warning on `truck-body`.
2026-05-20: Provider support now includes OpenAI and Gemini. `modelConfig.ts` no longer has a hardcoded provider field; OpenAI keeps the starting default, while the Providers panel caches the last selected provider in localStorage. Local dev keys are served only through `/__manifest3d/local-provider-api-keys` from `.env` (`OPENAI_API_KEY`/legacy `VITE_OPENAI_API_KEY`, `GEMINI_API_KEY`/`GOOGLE_API_KEY`), and non-local origins keep per-provider API keys only in tab memory. Gemini uses the direct GenerateContent REST path with `gemini-flash-latest`, `thinkingLevel: "high"`, temperature 1.0, max output tokens 64k, and the shared Manifest3D JSON schema via `generationConfig.responseMimeType` plus `responseJsonSchema`.
2026-05-20: Pending create rows in the asset history panel should show the literal submitted prompt as their temporary title, with no multi-run numeric suffix; once saved, the normal asset name/version row takes over. Animation preview playback is now per-control, so multiple joint/material bars can play together, while an individual reset stops only that bar.
2026-05-21: Material render side is now an explicit manifest material contract (`front`/`back`/`double`) and the WebGPU renderer maps it directly to Three material side. Missing legacy side defaults to `front`, while strict provider output requires it. Open/cutaway `lathe` visuals are treated as surface-side sensitive and need a matching `expect_material_side` authored check, so paper-thin or one-sided surfaces remain allowed but intentional/testable instead of accidental back-face culling.
2026-05-21: Agent runs remain stateless at the provider transcript layer, but saved asset versions now optionally persist direct submitted user input (`text` plus attached image data/metadata). Follow-up edit prompts prepend selected-version lineage user inputs plus the pending turn before the existing prompt assembly and send all referenced images with the fresh provider request. Saved right-panel transcripts are reconstructed from version lineage when user input exists; legacy rows without user input intentionally keep the old attempt-only display.
2026-05-22: Viewport world light/dark mode is renderer-scoped: `viewportWorld.ts` owns the bright existing world and the low-light dark world, while the top-left icon control only moves with the left asset panel. Do not tie this to app UI theming or mutate asset materials for the mode.
