read [AGENTS.md](.codex/AGENTS.md) for project rules. the app is currently finished for all phases planned in [init_plan.md](dev_docs/init_plan.md) . your goal is to run automated pipeline improvement. We have already done a couple of such automated pipeline improvement goal runs already, so improvements going forward will (likely but not always) be marginal in nature, but any improvements even marginal will compound to both better output asset quality and better reliability of the pipeline. details:

1. don't touch UI element (if you do find a pipeline improvements that does require some UI adjustment, let me know first and pending approval, generally consider any UI or peripheral changes to be out of scope in this task)
2. the app has an embedded engine and agent harness. this is the scope of this goal run. the agent generates 3d asset and authored test, the engine runs general QC and authored test, the harness manages iterations to resolve test issues and eventually delivers an all-passed 3d asset
3. your job is to continuously improve this exact pipeline, the objective is not one dimensional. your main goal is to improve the final quality of the output 3d asset through improvement of the engine and agent harness, right now the generated 3d asset from current engine and harness are good, but not necessarily great. considering the engine and harness is roughly copying the reference project Articraft, i expect this pipeline to produce at least as good 3d assets as articraft; your secondary goal is to strike a balance between final 3d asset quality and latency (currently run per asset takes easily 10+ mins and sometimes even a few times longer than that, so if you can optimize latency to be better than this without sacrificing output quality, go for it; but if not, at least aim to not make latency worse).
4. everything you can read to get a better grip of the task:
    - current codebase -- primary source of truth
    - docs and notes in dev_docs/ , these are artifacts from the implementation that led to the code you're reading now, they might give you more info like the intention and design choices beyond the code itself
    - the reference Articraft project -- this is your theoretical reference baseline
5. under test/headless/ , there is a headless run shim that pools all the logic of the pipeline embedded in the app and run them as the app would but headlessly to test every part of the pipeline, in order to diagnose failure modes, pain points, frictions, etc., for the sake of improving the entire pipeline. you can already see some previous headless run results in test/. besides monitoring the run back-and-forth interactions and metric data, also use browser to inspect the headless run glb output to visually verify the quality of the final 3d asset
6. since we're treating Articraft as a theoretical baseline, here in the attach image: [articraft_ref_imagery](/refs/articraft_ref.jpeg). You can see the assets that articraft can create, the level of quality that it can reach, they look better and more refined than the assets manifest3d currently can produce (it varies asset to asset, but overall i'd say it's not producing assets with the same quality as articraft). so when you visually inspecting headless test results, have this image as a visual baseline reference

one thing i want to stress is that when you're patching and expanding the headless test (if needed), do not bend the project around the test, instead bend the test code around the app code, plumbing for headless test should be self-contained in test/headless/agentPipelineSmoke.test.ts and its README/artifacts, including any Node-only FileReader shim. App src changes will only be for actual engine/harness quality behavior, not to make the smoke test easier.

for running the headless tests, the idea is to practically stress test the pipeline, so try both simple prompts and difficult prompts, both static assets (ie without any moving parts) and dynamic assets (ie with moving part), you can even try requests with a reference image for the asset as input (generate with your image gen tool) along with text prompt to stress test the pipeline. current timeout is set 1h, so just let the headless tests run, don't cut it off or interrupt, the point is to simulate real runs and improve the pipeline, not to performatively pass the test

make sure you update relevant docs in dev_docs/ too in the end on your improvements of the engine and harness to replace stale references

now kick off this automated manifest3d pipeline improvement run